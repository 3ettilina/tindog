import 'package:flutter/material.dart';

class OnboardingLocationRequest extends StatelessWidget {
  const OnboardingLocationRequest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
